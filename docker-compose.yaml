nginx-proxy:
    container_name: nginx-proxy
    image: nginx:latest
    ports:
      - 80:80
    volumes:
      - ./conf/:/etc/nginx/
      - ./includes/:/etc/nginx/includes/
    networks:
      - container-network
